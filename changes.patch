diff --git a/index.html b/index.html
index d9a03f3..8af227a 100644
--- a/index.html
+++ b/index.html
@@ -13,6 +13,16 @@
     <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/gsap.min.js"></script>
     <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/ScrollTrigger.min.js"></script>
     <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,700;1,700&family=Plus+Jakarta+Sans:wght@300;400;600&display=swap" rel="stylesheet">
+    
+    <!-- Gaussian Splatting 3D Viewer -->
+    <script type="importmap">
+    {
+        "imports": {
+            "three": "https://unpkg.com/three@0.160.0/build/three.module.js",
+            "gaussian-splats-3d": "https://unpkg.com/@mkkellogg/gaussian-splats-3d@0.4.0/build/gaussian-splats-3d.module.js"
+        }
+    }
+    </script>
     <style>
         :root {
             --maori-red: #B12B21;
@@ -153,7 +163,7 @@
                 Digital (v2) <br><span class="italic text-gradient font-serif">Resurrection</span>
             </h1>
             <p class="text-lg md:text-xl text-white/60 max-w-3xl mx-auto mb-12 font-light tracking-wide opacity-0" id="hero-desc">
-                Pioneering the 3D digital reconstruction, preservation, and interactive activation of cultural heritage. 
+                Specialists in photogrammetry, 3D fine modeling, and digital heritage preservation. 
                 Bridging ancient wisdom with future technology to bring history back to life.
             </p>
             <div class="flex flex-col md:flex-row items-center justify-center gap-6 opacity-0" id="hero-cta">
@@ -181,7 +191,14 @@
             <div class="grid lg:grid-cols-12 gap-12 items-center">
                 <div class="lg:col-span-7 relative group">
                     <div class="absolute -inset-4 bg-gradient-to-tr from-[#B12B21]/20 to-transparent rounded-[2.5rem] blur-2xl opacity-0 group-hover:opacity-100 transition-opacity duration-1000"></div>
-                    <div id="viewer-canvas" class="relative z-10 glass-effect"></div>
+                    <div id="viewer-canvas" class="relative z-10 glass-effect">
+                        <div id="splat-loader" class="absolute inset-0 flex items-center justify-center bg-black/50 backdrop-blur-sm rounded-[2rem] z-20">
+                            <div class="text-center">
+                                <div class="w-10 h-10 border-2 border-[#B12B21] border-t-transparent rounded-full animate-spin mx-auto mb-4"></div>
+                                <p class="text-xs uppercase tracking-widest text-white/50">Loading 3D Model...</p>
+                            </div>
+                        </div>
+                    </div>
                     
                     <div class="absolute top-8 left-8 z-20 flex flex-col space-y-2">
                         <div class="bg-black/80 backdrop-blur px-4 py-2 rounded-lg border border-white/10">
@@ -193,9 +210,9 @@
 
                 <div class="lg:col-span-5 space-y-10 pl-0 lg:pl-10">
                     <div>
-                        <h3 class="text-3xl font-bold mb-6">Ancient Wisdom <br>Decoded by Intelligence</h3>
+                        <h3 class="text-3xl font-bold mb-6">Rock Art Heritage <br>Preserved in 3D</h3>
                         <p class="text-white/50 leading-relaxed mb-8">
-                            At **Laniakea Intelligence**, we treat every site as a living data-set. Our reconstruction process honors the *Mana* (prestige) of the ancestors by ensuring every digital grain of wood is captured with absolute fidelity.
+                            Using advanced photogrammetry and Gaussian splatting technology, we capture cultural heritage with sub-millimeter precision. Every texture, pigment, and crack is digitally preserved for future generations.
                         </p>
                     </div>
                     
@@ -222,8 +239,8 @@
     <section id="technology" class="py-32 bg-black overflow-hidden">
         <div class="container mx-auto px-6">
             <div class="flex flex-col md:flex-row justify-between items-end mb-24">
-                <h2 class="text-5xl font-bold max-w-xl">Intelligent <span class="italic text-[#B12B21]">Sovereignty</span></h2>
-                <p class="text-white/40 max-w-sm mt-8 md:mt-0">Laniakea Intelligence partners with Iwi to provide a technological bridge between past and future.</p>
+                <h2 class="text-5xl font-bold max-w-xl">Our <span class="italic text-[#B12B21]">Technology</span></h2>
+                <p class="text-white/40 max-w-sm mt-8 md:mt-0">State-of-the-art photogrammetry and 3D modeling for government heritage projects.</p>
             </div>
 
             <div class="grid md:grid-cols-3 gap-8">
@@ -233,9 +250,9 @@
                     <img src="https://images.unsplash.com/photo-1518709268805-4e9042af9f23?auto=format&fit=crop&q=80&w=800" class="absolute inset-0 w-full h-full object-cover transition-transform duration-1000 group-hover:scale-110" alt="Scanning">
                     <div class="relative z-20">
                         <span class="text-xs font-bold uppercase mb-2 block">Capture</span>
-                        <h4 class="text-2xl font-bold mb-4">Spatial Harvesting</h4>
+                        <h4 class="text-2xl font-bold mb-4">Photogrammetry</h4>
                         <p class="text-white/70 text-sm opacity-0 group-hover:opacity-100 transition-all duration-500 translate-y-4 group-hover:translate-y-0">
-                            Using industrial LiDAR and photogrammetry to harvest billion-point clouds of historic Pa sites.
+                            Using multi-angle photography and advanced photogrammetry software to create precise 3D point clouds.
                         </p>
                     </div>
                 </div>
@@ -245,9 +262,9 @@
                     <img src="https://images.unsplash.com/photo-1451187580459-43490279c0fa?auto=format&fit=crop&q=80&w=800" class="absolute inset-0 w-full h-full object-cover transition-transform duration-1000 group-hover:scale-110" alt="AI">
                     <div class="relative z-20">
                         <span class="text-xs font-bold uppercase mb-2 block">Process</span>
-                        <h4 class="text-2xl font-bold mb-4">Cognitive Mesh</h4>
+                        <h4 class="text-2xl font-bold mb-4">3D Fine Modeling</h4>
                         <p class="text-white/70 text-sm opacity-0 group-hover:opacity-100 transition-all duration-500 translate-y-4 group-hover:translate-y-0">
-                            Proprietary AI algorithms that reconstruct missing architectural elements based on tribal records.
+                            Sub-millimeter precision mesh generation and Gaussian splatting for hyper-realistic digital twins.
                         </p>
                     </div>
                 </div>
@@ -257,9 +274,9 @@
                     <img src="https://images.unsplash.com/photo-1550745165-9bc0b252726f?auto=format&fit=crop&q=80&w=800" class="absolute inset-0 w-full h-full object-cover transition-transform duration-1000 group-hover:scale-110" alt="Archive">
                     <div class="relative z-20">
                         <span class="text-xs font-bold uppercase mb-2 block">Preserve</span>
-                        <h4 class="text-2xl font-bold mb-4">Sky-Vault Archive</h4>
+                        <h4 class="text-2xl font-bold mb-4">Digital Archive</h4>
                         <p class="text-white/70 text-sm opacity-0 group-hover:opacity-100 transition-all duration-500 translate-y-4 group-hover:translate-y-0">
-                            Secure, tribal-owned digital vaults ensuring that cultural data never leaves the hands of its people.
+                            Secure long-term storage and interactive web delivery for government heritage preservation.
                         </p>
                     </div>
                 </div>
@@ -272,8 +289,8 @@
         <div class="absolute left-0 bottom-0 w-full h-1/2 bg-gradient-to-t from-[#B12B21]/10 to-transparent"></div>
         <div class="container mx-auto px-6 relative z-10">
             <div class="text-center mb-24">
-                <h2 class="text-6xl md:text-8xl font-bold mb-8 italic">Activate the <br><span class="text-gradient">Ancestral Path</span></h2>
-                <p class="text-xl text-white/50 mb-12">Laniakea Intelligence: 3D Reconstruction, Preservation, and Vitality of Cultural Heritage.</p>
+                <h2 class="text-6xl md:text-8xl font-bold mb-8 italic">Preserve Our <br><span class="text-gradient">Heritage</span></h2>
+                <p class="text-xl text-white/50 mb-12">Laniakea Intelligence: Digital Heritage Solutions for Government & Community.</p>
                 <a href="mailto:consult@laniakea.co.nz" class="text-3xl md:text-5xl font-light hover:text-[#B12B21] transition-colors underline underline-offset-8">consult@laniakea.co.nz</a>
             </div>
             
@@ -356,73 +373,100 @@
             });
         }
 
-        // --- Viewer Three.js (Carved Pou) ---
-        function initViewerScene() {
+        // --- Viewer Three.js (Gaussian Splatting 3D Rock Art) ---
+        async function initViewerScene() {
             const container = document.getElementById('viewer-canvas');
+            const loader = document.getElementById('splat-loader');
+            
+            // Remove the old simple 3D geometry setup, now using Gaussian Splatting
             const scene = new THREE.Scene();
-            const camera = new THREE.PerspectiveCamera(50, container.clientWidth / container.clientHeight, 0.1, 1000);
-            const renderer = new THREE.WebGLRenderer({ antialias: true, alpha: true });
+            const camera = new THREE.PerspectiveCamera(65, container.clientWidth / container.clientHeight, 0.1, 500);
+            
+            // Camera positions for rock art view
+            const initialPos = new THREE.Vector3(0, 0, 5);
+            const target = new THREE.Vector3(0, 0, 0);
+            camera.position.copy(initialPos);
+            camera.lookAt(target);
+
+            const renderer = new THREE.WebGLRenderer({ 
+                antialias: true, 
+                alpha: true,
+                powerPreference: "default"
+            });
             renderer.setSize(container.clientWidth, container.clientHeight);
+            renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2));
+            
+            // Clear container and add renderer
+            while(container.firstChild) {
+                if (container.firstChild.id === 'splat-loader') break;
+                container.removeChild(container.firstChild);
+            }
             container.appendChild(renderer.domElement);
 
-            const group = new THREE.Group();
-            
-            // Core
-            const baseGeo = new THREE.CylinderGeometry(0.5, 0.6, 4, 32);
-            const woodMat = new THREE.MeshStandardMaterial({ 
-                color: 0x1a0a0a,
-                roughness: 0.9,
-                metalness: 0.0
-            });
-            const core = new THREE.Mesh(baseGeo, woodMat);
-            group.add(core);
-
-            // Artistic Detail
-            const knotGeo = new THREE.TorusKnotGeometry(0.7, 0.15, 128, 16, 2, 3);
-            const detailMat = new THREE.MeshStandardMaterial({ 
-                color: 0xB12B21, 
-                wireframe: true,
-                transparent: true,
-                opacity: 0.4
+            // Import Gaussian Splats 3D
+            const { default: GaussianSplats3D } = await import('https://unpkg.com/@mkkellogg/gaussian-splats-3d@0.4.0/build/gaussian-splats-3d.module.js');
+
+            const viewer = new GaussianSplats3D.Viewer({
+                'showStatus': false,
+                'sharedMemoryForWorkers': false,
+                'selfContained': false,
+                'renderer': renderer,
+                'camera': camera,
+                'scene': scene,
+                'useBuiltInControls': false
             });
-            const knot = new THREE.Mesh(knotGeo, detailMat);
-            knot.position.y = 1.2;
-            group.add(knot);
-
-            const knot2 = knot.clone();
-            knot2.position.y = -1.2;
-            knot2.rotation.x = Math.PI;
-            group.add(knot2);
-
-            scene.add(group);
 
-            // Lighting
-            const p1 = new THREE.PointLight(0xB12B21, 2, 10); p1.position.set(2, 2, 2); scene.add(p1);
-            const p2 = new THREE.PointLight(0xD4AF37, 1.5, 10); p2.position.set(-2, -2, 2); scene.add(p2);
-            scene.add(new THREE.AmbientLight(0xffffff, 0.2));
+            // Load the rock art splat model
+            await viewer.addSplatScene('./rock_art.splat', {
+                'progressiveLoad': true,
+                'showLoadingUI': false,
+                'position': [0, -1, 0],
+                'rotation': [0, 0, 0, 1],
+                'scale': [1.5, 1.5, 1.5]
+            });
 
-            camera.position.z = 6;
+            // Hide loader
+            if (loader) {
+                loader.style.opacity = '0';
+                setTimeout(() => loader.style.display = 'none', 500);
+            }
 
-            let mx = 0, my = 0;
-            container.addEventListener('mousemove', (e) => {
+            // Mouse interaction for parallax tilt
+            let mouseX = 0;
+            let mouseY = 0;
+            
+            document.addEventListener('mousemove', (e) => {
                 const r = container.getBoundingClientRect();
-                mx = ((e.clientX - r.left) / container.clientWidth) * 2 - 1;
-                my = -((e.clientY - r.top) / container.clientHeight) * 2 + 1;
+                if (e.clientX >= r.left && e.clientX <= r.right && 
+                    e.clientY >= r.top && e.clientY <= r.bottom) {
+                    mouseX = ((e.clientX - r.left) / r.width) * 2 - 1;
+                    mouseY = -((e.clientY - r.top) / r.height) * 2 + 1;
+                }
             });
 
             function animate() {
                 requestAnimationFrame(animate);
-                group.rotation.y += 0.005;
-                group.rotation.y += (mx * 0.4 - group.rotation.y) * 0.05;
-                group.rotation.x += (my * 0.4 - group.rotation.x) * 0.05;
-                renderer.render(scene, camera);
+                
+                // Parallax tilt effect
+                const tiltX = mouseX * 0.5;
+                const tiltY = mouseY * 0.5;
+                
+                camera.position.x = THREE.MathUtils.lerp(camera.position.x, initialPos.x + tiltX, 0.05);
+                camera.position.y = THREE.MathUtils.lerp(camera.position.y, initialPos.y - tiltY, 0.05);
+                camera.lookAt(target);
+
+                viewer.update();
+                viewer.render();
             }
             animate();
 
             window.addEventListener('resize', () => {
-                camera.aspect = container.clientWidth / container.clientHeight;
+                const width = container.clientWidth;
+                const height = container.clientHeight;
+                camera.aspect = width / height;
                 camera.updateProjectionMatrix();
-                renderer.setSize(container.clientWidth, container.clientHeight);
+                renderer.setSize(width, height);
+                viewer.resize();
             });
         }
     </script>
